# Tooling / agent scratch
.claude/
.impeccable/

# Local design previews (not part of the live site)
book-preview.html
hero-preview.html
intro-preview.html

# Internal planning notes
PRODUCT.md

# OS junk
.DS_Store

# Signing secrets — NEVER commit these
android-signing/
*.jks
*.keystore
keystore.properties

# Capacitor / Android app build cruft (never commit these)
android/
node_modules/
www/
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/DerivedData/
ios/capacitor-cordova-ios-plugins/
*.xcuserstate
.sourcemaps/

# Obsidian vault personal settings (keep the notes, ignore per-user config)
GreenShelf/.obsidian/
greenshelf/
